# F-096 / F-110 candidate wave

Status: candidate implementation; no parity score change.

- F-096 adds an explicit new/open Model-space document contract with independent state read-back.
- F-110 opens ASCII DXF as a new editable document, preserves declared linear units and core entity handles, and refuses partial imports by default.
- DXFIN into an existing document remains the certified F-111 path and is not changed.
- The independent file read-back exports the opened document again and parses the second-generation DXF through the production parser.

Certification still requires the row-specific Chromium and owned AutoCAD 2024.1.2 live workflows plus current-byte cross-evidence outside this workstream's file ownership.
