# Security policy

Report vulnerabilities privately to `info@kuubikprojekt.ee`. Do not attach
customer CAD files. Include the affected commit, a minimal synthetic fixture,
impact and reproduction steps.

Only supported pinned oracle versions may process fixtures. Oracle processes
run in disposable directories with timeouts and no production document access.
